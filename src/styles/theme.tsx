
import { extendTheme } from "@chakra-ui/react"

const theme = extendTheme({
  colors: {
    main: {
      100: "#b592fd"
    },
    frost: {
      100: "#fff",
      200: "#e6e6e6",
      300: "#9A9A9A",
      400: "#000"
    }
  },
})

export default theme;